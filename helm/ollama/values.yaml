ollama:
  gpu:
    enabled: true
    type: nvidia
    number: 1
  models:
    - gemma4:31b

persistentVolume:
  enabled: true
  size: 50Gi

nodeSelector:
  kubernetes.io/hostname: swipe-worker-3

resources:
  limits:
    nvidia.com/gpu: 1
